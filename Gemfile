# frozen_string_literal: true

source 'https://rubygems.org'

# FIXME: puppet-6.15.0 introduced a regression with running playbooks.
#
# For this reason, mco and puppet are included in this project bundle so that
# we can work around that regression with `bundle exec mco ...`
#
# See https://tickets.puppetlabs.com/browse/PUP-10397
gem 'choria-mcorpc-support'
gem 'puppet', '6.14.0'

gem 'r10k'
